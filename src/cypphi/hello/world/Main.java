package cypphi.hello.world;

import cypphi.hello.world.utils.LoggerUtil;
import cypphi.hello.world.utils.TrueStatementCheckerUtil;

public class Main {
    public static boolean isTrue = BooleanStatements.trueStatement;

    public static void main(String[] args) {
        if (isTrue = BooleanStatements.trueStatement) {
            if (TrueStatementCheckerUtil.isActuallyTrue = !BooleanStatements.falseStatement) {
                LoggerUtil.Log(SpecialCharacters.space + HelloWorld.helloWord + SpecialCharacters.space + HelloWorld.worldWord + SpecialCharacters.exclamationMark);
            }
        }
    }
}
